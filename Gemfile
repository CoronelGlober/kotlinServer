source "https://rubygems.org"

group :fastlane, optional: true do
  gem 'fastlane'
#  gem 'nokogiri', '>= 1.13.9'

  # all fastlane plugins need to be loaded with fastlane or else
  # fastlane will complain about missing gems when it runs.
  gem 'fastlane-plugin-appcenter'
 # gem 'fastlane-plugin-bugsnag'
end